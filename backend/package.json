{
  "name": "legal-case-backend",
  "version": "1.0.0",
  "description": "Backend for Legal Case Management System",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "migrate": "node migrations/run.js",
    "neon:create": "neonctl projects create --name legal-case-db",
    "neon:connection": "neonctl connection-string --project-id $(neonctl projects list --output json | jq -r '.[0].id')",
    "neon:branch": "neonctl branches create --project-id $(neonctl projects list --output json | jq -r '.[0].id') --name main"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.13",
    "pg": "^8.13.0",
    "uuid": "^10.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "keywords": [
    "legal",
    "case",
    "management"
  ]
}
